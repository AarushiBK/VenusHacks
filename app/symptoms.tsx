import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function SymptomTracker() {
  return (
    <ScreenPlaceholder
      title="Symptom Tracker"
      description="Log and review today's symptoms and trends."
    />
  );
}
